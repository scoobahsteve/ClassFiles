boolean = false;

var unique = function (arr, dup) {
  arr = string.split;
  dup = {};

  for (var i = 0; i < arr.length; i++) {
    if (found.in.arr = [i]) {
      return false;
    } else {
      boolean = true;
    }
  }
};
